<%= form_for(@flux) do |f| %>
  <% if @flux.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flux.errors.count, "error") %> prohibited this flux from being saved:</h2>

      <ul>
      <% @flux.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
